export const metadata = {
  title: 'Pricing',
  description: 'Transparent pricing for photo editing, video editing, and studio rental at Brand Grow Studio. Starting from ৳500 BDT.',
};

export default function PricingLayout({ children }) {
  return children;
}
